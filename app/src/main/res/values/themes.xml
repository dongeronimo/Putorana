<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. NoActionBar: the renderer owns the whole
         window, there is no chrome to put above it. -->
    <style name="Theme.ARReconstructor" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- The system bars are hidden at runtime, but they still come back
             transiently on an edge swipe. Transparent, with the contrast scrims
             off, so nothing ever paints an opaque strip over the surface.
             Ignored on API 35+, where the platform forces this anyway; they are
             what matters on 33 and 34. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>

        <!-- Let the surface extend under a notch or punch-hole instead of the
             window being letterboxed away from it. Switch to "always" if the
             renderer should also reach the cutout in landscape. -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
